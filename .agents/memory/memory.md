# 專案記憶 (Project Memory)

## 已完成進度：
- **專案初始化**：
  - 建立 Flask 基礎的 `app.py` 檔案（包含 Hello World 路由）。
  - 使用 `python -m venv venv` 建立虛擬環境。
  - 於虛擬環境中安裝 Flask 套件。
  - 建立 `.agents/memory/` 與 `.agents/rules/` 目錄結構。
  - 執行 `git init` 初始化專案版本控制。
- **專案規劃階段**：
  - 接收並分析使用者提供的「防詐騙模擬網站」情境與架構需求。
  - 依照使用者需求，於計畫中追加前置「測試詐騙頁面」。
  - 提交計畫書並經使用者同意，進入實作階段。
- **專案實作階段**：
  - 建立 `task.md` 任務清單。
  - 建立 `init_db.py` 準備初始化測試用資料庫。
  - 已完成資料庫初始化與測試資料寫入。
  - 完成 `app.py` 路由與邏輯實作。
  - 完成所有 HTML 模板與專屬 CSS 的開發。
  - 更新任務狀態為完成，並提交 `walkthrough.md`。
  - 新增 `app.py` 的根目錄 (`/`) 自動導向至 `/mock_scam` 以修正 404 錯誤。
  - 根據使用者要求，從 `mock_scam.html` 中移除了「例外測試 (模組 D)」的相關按鈕與區塊。
  - 修改 `ig_login.html` 的「使用 Facebook 登入」按鈕，使其能導向至 `/fb` 並攜帶相同的 `id` 參數。
  - 將教育頁面的「重新開始演練」按鈕改為「更多資訊」，點擊後導向至新的 `scammed_again.html` 頁面，顯示「你又被騙了」警告並附上內政部警政署 165 全民防騙網連結。
  - 在 `scammed_again.html` 頁面中將背景影片更換為 `boss_bg.mp4`，並將提示紅框調小、增加透明度與毛玻璃效果，以避免過度遮擋背後的影片。
